#!/usr/bin/env bash

for FILE in bash_login bashrc gemrc gitignore_global gvimrc inputrc irbrc osx profile rvmrc tmux.conf vimrc zlogin zshrc; do
  if [[ -s "$HOME/.$FILE" ]]; then
    echo "$HOME/.$FILE already exists, deleting."
    rm -f "$HOME/.$FILE"
  fi
done
