# Ignore everything in bin except for my stuff - users need to be able to place
# their own scripts here.
/bin/*
!/bin/dcp-*
!/bin/yolo

# lib directory so I can sneak PyPI packages onto my CSE account
/lib/*

# private contains your custom aliases/environment variables
private
# theme contains just the name of a custom theme
theme
# friends contains your FB friends list (if you use dcp-facebook-friends)
friends
