# Load aliases
[[ -e "${DCP}/aliasrc" ]] && source "${DCP}/aliasrc"

# Load Mac aliases, if appropriate
if [[ "${DCP_OS}" = "Darwin" ]]; then
  [[ -e "${DCP}/aliasrc_osx" ]] && source "${DCP}/aliasrc_osx"
fi

# Load local modifications
[[ -e "${DCP}/localrc" ]] && source "${DCP}/localrc"
