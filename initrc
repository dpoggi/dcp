# Load aliases
[[ -e "${HOME}/.dcp/aliasrc" ]] &&
  source "${HOME}/.dcp/aliasrc"
# Load local scripts
[[ -e "${HOME}/.dcp/localrc" ]] &&
  source "${HOME}/.dcp/localrc"
# Load environment
[[ -e "${HOME}/.dcp/envrc" ]] &&
  source "${HOME}/.dcp/envrc"
