readonly DCP="${HOME}/.dcp"

# Set up environment
. "${DCP}/lib/env.sh"

# Set up completion
. "${DCP}/lib/bash_completion.sh"

# PS1
. "${DCP}/lib/ps1.sh"

set_prompt

# Load most of dcp
. "${DCP}/lib/init.sh"

# Finish up the details
. "${DCP}/lib/post_init.sh"
