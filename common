[[ -s "$HOME/.dcp/initrc" ]] &&
  source "$HOME/.dcp/initrc"
[[ -s "$HOME/.dcp/private" ]] &&
  source "$HOME/.dcp/private"

if [[ `uname` != "SunOS" ]]; then
  if [[ -s "$HOME/.rbenv/bin/rbenv" ]]; then
    export PATH="$HOME/.rbenv/bin:$PATH"
    eval "$(rbenv init -)"
  elif [[ -s "$HOME/.rvm/scripts/rvm" ]]; then
    source "$HOME/.rvm/scripts/rvm"
  fi
fi

export PATH="$PATH:$HOME/.dcp/bin"
export EDITOR="vim"
